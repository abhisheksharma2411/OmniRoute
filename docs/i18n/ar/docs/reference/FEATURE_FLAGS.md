# Feature Flags (العربية)

🌐 **Languages:** 🇺🇸 [English](../../../../reference/FEATURE_FLAGS.md) · 🇪🇹 [am](../../../am/docs/reference/FEATURE_FLAGS.md) · 🇦🇿 [az](../../../az/docs/reference/FEATURE_FLAGS.md) · 🇧🇬 [bg](../../../bg/docs/reference/FEATURE_FLAGS.md) · 🇧🇩 [bn](../../../bn/docs/reference/FEATURE_FLAGS.md) · 🇨🇿 [cs](../../../cs/docs/reference/FEATURE_FLAGS.md) · 🇩🇰 [da](../../../da/docs/reference/FEATURE_FLAGS.md) · 🇩🇪 [de](../../../de/docs/reference/FEATURE_FLAGS.md) · 🇬🇷 [el](../../../el/docs/reference/FEATURE_FLAGS.md) · 🇪🇸 [es](../../../es/docs/reference/FEATURE_FLAGS.md) · 🇪🇪 [et](../../../et/docs/reference/FEATURE_FLAGS.md) · 🇮🇷 [fa](../../../fa/docs/reference/FEATURE_FLAGS.md) · 🇫🇮 [fi](../../../fi/docs/reference/FEATURE_FLAGS.md) · 🇫🇷 [fr](../../../fr/docs/reference/FEATURE_FLAGS.md) · 🇮🇪 [ga](../../../ga/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [gu](../../../gu/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [ha](../../../ha/docs/reference/FEATURE_FLAGS.md) · 🇮🇱 [he](../../../he/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [hi](../../../hi/docs/reference/FEATURE_FLAGS.md) · 🇭🇷 [hr](../../../hr/docs/reference/FEATURE_FLAGS.md) · 🇭🇺 [hu](../../../hu/docs/reference/FEATURE_FLAGS.md) · 🇦🇲 [hy](../../../hy/docs/reference/FEATURE_FLAGS.md) · 🇮🇩 [id](../../../id/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [ig](../../../ig/docs/reference/FEATURE_FLAGS.md) · 🇮🇹 [it](../../../it/docs/reference/FEATURE_FLAGS.md) · 🇯🇵 [ja](../../../ja/docs/reference/FEATURE_FLAGS.md) · 🇬🇪 [ka](../../../ka/docs/reference/FEATURE_FLAGS.md) · 🇰🇭 [km](../../../km/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [kn](../../../kn/docs/reference/FEATURE_FLAGS.md) · 🇰🇷 [ko](../../../ko/docs/reference/FEATURE_FLAGS.md) · 🇱🇹 [lt](../../../lt/docs/reference/FEATURE_FLAGS.md) · 🇱🇻 [lv](../../../lv/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [ml](../../../ml/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [mr](../../../mr/docs/reference/FEATURE_FLAGS.md) · 🇲🇾 [ms](../../../ms/docs/reference/FEATURE_FLAGS.md) · 🇲🇹 [mt](../../../mt/docs/reference/FEATURE_FLAGS.md) · 🇲🇲 [my](../../../my/docs/reference/FEATURE_FLAGS.md) · 🇳🇵 [ne](../../../ne/docs/reference/FEATURE_FLAGS.md) · 🇳🇱 [nl](../../../nl/docs/reference/FEATURE_FLAGS.md) · 🇳🇴 [no](../../../no/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [or](../../../or/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [pa](../../../pa/docs/reference/FEATURE_FLAGS.md) · 🇵🇭 [phi](../../../phi/docs/reference/FEATURE_FLAGS.md) · 🇵🇱 [pl](../../../pl/docs/reference/FEATURE_FLAGS.md) · 🇵🇹 [pt](../../../pt/docs/reference/FEATURE_FLAGS.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/reference/FEATURE_FLAGS.md) · 🇷🇴 [ro](../../../ro/docs/reference/FEATURE_FLAGS.md) · 🇷🇺 [ru](../../../ru/docs/reference/FEATURE_FLAGS.md) · 🇱🇰 [si](../../../si/docs/reference/FEATURE_FLAGS.md) · 🇸🇰 [sk](../../../sk/docs/reference/FEATURE_FLAGS.md) · 🇸🇮 [sl](../../../sl/docs/reference/FEATURE_FLAGS.md) · 🇷🇸 [sr](../../../sr/docs/reference/FEATURE_FLAGS.md) · 🇸🇪 [sv](../../../sv/docs/reference/FEATURE_FLAGS.md) · 🇰🇪 [sw](../../../sw/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [ta](../../../ta/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [te](../../../te/docs/reference/FEATURE_FLAGS.md) · 🇹🇭 [th](../../../th/docs/reference/FEATURE_FLAGS.md) · 🇹🇷 [tr](../../../tr/docs/reference/FEATURE_FLAGS.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/reference/FEATURE_FLAGS.md) · 🇵🇰 [ur](../../../ur/docs/reference/FEATURE_FLAGS.md) · 🇺🇿 [uz](../../../uz/docs/reference/FEATURE_FLAGS.md) · 🇻🇳 [vi](../../../vi/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [yo](../../../yo/docs/reference/FEATURE_FLAGS.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/reference/FEATURE_FLAGS.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/reference/FEATURE_FLAGS.md)

---

> مفاتيح تبديل وقت التشغيل التي تغيّر سلوك OmniRoute **من دون إعادة النشر**.
> كل علامة مدرجة هنا معرّفة في
> [`src/shared/constants/featureFlagDefinitions.ts`](../../src/shared/constants/featureFlagDefinitions.ts)
> — وهو المصدر الوحيد للحقيقة. تقرأ كلٌّ من لوحة المعلومات وواجهة REST API من
> ذلك الملف، ولذلك يُنشأ الجدول أدناه ليطابقه بنسبة 1:1.

---

## ما هي علامات الميزات

علامة الميزة هي مفتاح تبديل مُسمّى (قيمة منطقية أو تعداد) يمكن تغيير قيمته في
وقت التشغيل وحفظها في قاعدة البيانات، من دون الحاجة إلى إعادة نشر العملية. تُوصَف كل
علامة بواسطة `FeatureFlagDefinition` يتضمن `key` و`label`
و`description` و`category` و`defaultValue` و`type` وتلميح `requiresRestart`.

### ترتيب تحديد القيمة

تُحدَّد **القيمة الفعلية** للعلامة بواسطة
[`resolveFeatureFlag()`](../../src/shared/utils/featureFlags.ts) وفق
ترتيب الأولوية التالي (تفوز الأولوية الأعلى):

1. **تجاوز قاعدة البيانات** — قيمة مخزنة في جدول `key_value` ضمن نطاق الأسماء
   `feature_flags` (تُعيَّن عبر لوحة المعلومات أو واجهة REST API).
2. **متغير البيئة** — `process.env[<KEY>]`، إذا كان معيّنًا وغير فارغ.
3. **القيمة الافتراضية للتعريف** — قيمة `defaultValue` من `featureFlagDefinitions.ts`.

تُعد العلامة المنطقية **مفعّلة** عندما تكون قيمتها الفعلية `"true"` أو
`"1"` أو `"yes"` (راجع `isFeatureFlagEnabled()`).

> [!NOTE]
> لمعظم العلامات أيضًا متغير بيئة مطابق يحمل **الاسم نفسه**
> وموثّق في [`ENVIRONMENT.md`](./ENVIRONMENT.md). تكون الأولوية لتجاوز قاعدة البيانات الخاص بالعلامة
> على متغير البيئة هذا. تُحفَظ العلامة التي تحتوي على
> `requiresRestart: true` فورًا، لكنها لا تُقرأ مجددًا إلا عند بدء تشغيل العملية
> — ويؤدي تبديلها إلى إظهار لافتة **"إعادة تشغيل الخادم"** في لوحة المعلومات.

---

## دليل العلامات

72 علامة موزعة على 6 فئات. **القيمة الافتراضية** هي القيمة الافتراضية للتعريف — أي القيمة
المستخدمة عند عدم وجود تجاوز في قاعدة البيانات أو متغير بيئة.

### الأمان (10)

| المفتاح                                 | النوع | القيمة الافتراضية | الوصف                                                                                                                                                                                                                                                                  |
| --------------------------------------- | ----- | ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `REQUIRE_API_KEY`                       | منطقي | `false`           | طلب مفتاح API لجميع الطلبات الواردة.                                                                                                                                                                                                                                   |
| `INPUT_SANITIZER_ENABLED`               | منطقي | `true`            | تفعيل تنقية المدخلات لجميع الطلبات.                                                                                                                                                                                                                                    |
| `INJECTION_GUARD_MODE`                  | تعداد | `off`             | وضع الحماية من حقن المطالبات. القيم: `off` و`warn` و`block` و`redact`.                                                                                                                                                                                                 |
| `PII_REDACTION_ENABLED`                 | منطقي | `false`           | حجب معلومات التعريف الشخصية من الطلبات (بشكل مستقل عن `INPUT_SANITIZER_MODE`).                                                                                                                                                                                         |
| `PII_RESPONSE_SANITIZATION`             | منطقي | `false`           | تنقية معلومات التعريف الشخصية من استجابات مزوّد الخدمة.                                                                                                                                                                                                                |
| `PII_RESPONSE_SANITIZATION_MODE`        | تعداد | `redact`          | وضع تنقية معلومات التعريف الشخصية في الاستجابات. القيم: `redact` و`warn` و`block` و`off`.                                                                                                                                                                              |
| `OUTBOUND_SSRF_GUARD_ENABLED`           | منطقي | `true`            | حظر الطلبات الصادرة إلى نطاقات عناوين IP الخاصة/الداخلية.                                                                                                                                                                                                              |
| `ALLOW_API_KEY_REVEAL`                  | منطقي | `false`           | السماح لمستخدمي لوحة المعلومات المصادَق عليهم بكشف مفاتيح API المخزنة بدلًا من رؤية القيم المقنّعة فقط.                                                                                                                                                                |
| `AUTH_LOG_INCLUDE_ACCOUNT_ID`           | منطقي | `false`           | تضمين بادئة الحساب في أسطر سجل AUTH (مثل "استخدام حساب <provider>: abc12345..."). يكون هذا معطّلًا افتراضيًا بحيث تُحجب معرّفات الحسابات من سجلات العمليات المشتركة/متعددة المستأجرين. وهو مستقل عن وضع تصحيح الأخطاء؛ فتبديل وضع تصحيح الأخطاء لا يكشف هذه المعرّفات. |
| `OMNIROUTE_OIDC_DISABLE_PASSWORD_LOGIN` | منطقي | `false`           | عند تفعيل OIDC، يُعطَّل تسجيل الدخول بكلمة المرور بحيث لا يمكن للمستخدمين المصادقة إلا عبر تسجيل الدخول الموحّد باستخدام OIDC. وعند تعطيله (وهو الإعداد الافتراضي)، يتوفر كلٌّ من تسجيل الدخول بكلمة المرور وOIDC.                                                     |

### الشبكة (15)

| المفتاح                                         | النوع   | الافتراضي | إعادة التشغيل | الوصف                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ----------------------------------------------- | ------- | --------- | ------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ENABLE_TLS_FINGERPRINT`                        | boolean | `false`   | ✓             | تمكين وضع التخفي ببصمة TLS.                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `AUDIO_REMOTE_PROVIDER_NODES`                   | boolean | `false`   |               | السماح لمسارات /v1/audio/* باستخدام عُقد موفّرين متوافقة مع OpenAI ومستضافة خارج localhost. يكون معطّلًا افتراضيًا — إذ إن توجيه الصوت إلى مضيف بعيد يغيّر هوية الخروج، ويجب أن يكون قرارًا صريحًا من المشغّل. عُقد الاسترجاع الحلقي مسموح بها دائمًا ولا تتأثر.                                                                                                                                                                                                     |
| `PROXY_AUTO_SELECT_ENABLED`                     | boolean | `false`   |               | عند عدم تعيين وكيل لاتصال، يتم تلقائيًا اختيار أول وكيل عامل من السجل. يكون معطّلًا افتراضيًا (وإلا فسيصبح أي وكيل في السجل بديلًا احتياطيًا عامًا — #3332).                                                                                                                                                                                                                                                                                                         |
| `OMNIROUTE_CONTROL_PLANE_PROXY_DIRECT_FALLBACK` | boolean | `false`   |               | السماح لتدفقات OAuth والتحقق من الموفّر بتجاوز وكيل مثبّت والاتصال مباشرةً عند فشل عمليات التحقق المسبق من إمكانية الوصول إلى الوكيل. يكون معطّلًا افتراضيًا لأن ذلك قد يغيّر عنوان IP للخروج.                                                                                                                                                                                                                                                                       |
| `NETWORK_ROTATION_SHARED_EGRESS_GUARD`          | boolean | `true`    |               | عند حدوث استثناء في الشبكة (انتهاء المهلة أو رفض الاتصال/إعادة تعيينه) لمنفّذ تدوير متعدد الحسابات، وعندما لا يملك الحساب المتعثر وكيلًا مخصصًا، تُطبّق فترة تهدئة قصيرة وتُتخطى الحسابات الأخرى التي لا تملك وكلاء لبقية الطلب بدلًا من إعادة محاولة كل حساب منها. يكون مفعّلًا افتراضيًا (آمن: لا يغيّر عنوان IP للخروج، بل يقلّل فقط مخاطر زمن الاستجابة/التهدئة للحسابات ذات الخروج المشترك). عطّله لاستعادة النشر الفوري عند أول استثناء صادر عن حساب بلا وكيل. |
| `PROXY_SKIP_RECENTLY_FAILED`                    | boolean | `false`   |               | تتوقف مجموعات الوكلاء وعملية التدوير لكل حساب في opencode عن إعادة تقديم وكيل فشل للتو (فشل فحص TCP بسبب الرفض، أو تلقّي 429 عبره) لمدة خاصة بكل عملية تتضاعف مع كل تكرار حتى حد أقصى. لا تُكتب أي حالة للوكيل؛ وعند استبعاد جميع المرشحين يبقى الاختيار دون تغيير. يكون معطّلًا افتراضيًا.                                                                                                                                                                          |
| `PROXY_POOL_EGRESS_OBSERVATION`                 | boolean | `false`   |               | إظهار عدد عناوين IP المرصودة للخروج التي خدمت أعضاء مجموعة وكلاء خلال آخر 24 ساعة، وعدد الاتصالات التي استخدمتها، أسفل مجموعة الوكلاء في لوحة المعلومات. للقراءة فقط، ويُحتسب من سجل الوكيل، ولا يُستخدم مطلقًا للتوجيه. يكون معطّلًا افتراضيًا.                                                                                                                                                                                                                     |
| `OPENCODE_RESPONSES_STALL_ROTATION`             | boolean | `false`   |               | بالنسبة إلى منفّذ OpenCode، راقب أول بايت من النص الأساسي لاستجابة Responses متدفقة (النافذة: `RESPONSES_FIRST_BYTE_TIMEOUT_MS`، والقيمة الافتراضية `15000`). يُعامل تدفق Responses ذي حالة 2xx الذي يظل صامتًا بعد انقضاء النافذة على أنه متوقف: يُوضع الحساب في فترة تهدئة ويُدوّر الطلب إلى الحساب التالي مرة واحدة؛ ويؤدي توقف ثانٍ إلى فشل سريع. يكون معطّلًا افتراضيًا: تحتفظ التدفقات المتوقفة بمدة الانتظار الحالية حتى انتهاء مهلة جاهزية التدفق.           |
| `OPENCODE_USER_BLOCKED_ROTATION`                | boolean | `false`   |               | منفّذ OpenCode: عند ورود 403/451 يحمل رفضًا من نوع `user_blocked` (ليس متعلقًا بالموقع الجغرافي، وليس رفضًا لبصمة Cloudflare)، ضع الحساب المرفوض في فترة تهدئة ودوّر إلى الحساب التالي مرة واحدة كحد أقصى لكل طلب؛ ويُعاد الرفض الثاني كما هو، دون علامة نجاح. يكون معطّلًا افتراضيًا: قد يبدو الالتفاف في التوجيه حول حظر مستخدم من المنبع كنوع من المراوغة، وقد ينشر العلامة عبر كامل الأسطول.                                                                     |
| `OPENCODE_TRANSIENT_FAILOVER_BACKOFF`           | boolean | `false`   |               | تدوير OpenCode: بعد فشلين عابرين متتاليين من المنبع (5xx أو 400 فارغ)، توقّف مؤقتًا قبل الانتقال إلى الحساب التالي — 1.5 ثانية تتضاعف مع كل فشل إضافي، بحد أقصى 6 ثوانٍ لكل توقف و10 ثوانٍ لكل طلب، ويُتخطى ذلك عند قطع العميل للاتصال؛ ويُحرر النص الأساسي الفاشل قبل الانتظار. يكون معطّلًا افتراضيًا: يظل تجاوز الفشل فوريًا.                                                                                                                                     |
| `OPENCODE_RATE_LIMITED_429_EARLY_STOP`          | boolean | `false`   |               | تدوير OpenCode: أوقف موجة الحسابات عند أول 429 يُصنّف على أنه حد فعلي لمعدل الطلبات (`Retry-After` قابل للتحليل، أو نص أساسي يذكر حدًا للمعدل/الاستخدام)، وأعد استجابة 429 الواردة من المنبع دون تغيير. تستمر الاستجابات 429 غير المصنّفة في التدوير. يكون معطّلًا افتراضيًا: تُقيّد الفئة المجانية لكل عنوان IP للخروج (#9611)، لذا تؤدي كل استجابة 429 إلى التدوير، وتعيد الموجة المستنفدة آخر استجابة 429 من المنبع.                                              |
| `MITM_DISABLE_TLS_VERIFY`                       | boolean | `false`   | ✓             | تعطيل التحقق من شهادة TLS لوكيل MITM. **خطر.**                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `OMNIROUTE_ALLOW_PRIVATE_PROVIDER_URLS`         | boolean | `false`   |               | السماح بعناوين URL للموفّرين التي تشير إلى شبكات خاصة/داخلية.                                                                                                                                                                                                                                                                                                                                                                                                        |
| `OMNIROUTE_ALLOW_LOCAL_PROVIDER_URLS`           | boolean | `true`    |               | السماح بإضافة الموفّرين أو التحقق منهم على العناوين المحلية/الخاصة (127.0.0.1، localhost، LAN). يكون مفعّلًا افتراضيًا (المحلي أولًا)؛ عطّله لفرض الحظر الصارم الذي يسمح بالعناوين العامة فقط. تظل بيانات تعريف السحابة محظورة.                                                                                                                                                                                                                                      |
| `ENABLE_CC_COMPATIBLE_PROVIDER`                 | boolean | `false`   | ✓             | تمكين وضع الموفّر المتوافق مع Claude Code.                                                                                                                                                                                                                                                                                                                                                                                                                           |

### السياسات (5)

| المفتاح                         | النوع | القيمة الافتراضية | الوصف                                                                                                                                                                                                             |
| ------------------------------- | ----- | ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `TOOL_POLICY_MODE`              | تعداد | `disabled`        | وضع فرض سياسة استخدام الأدوات. القيم: `disabled`، `warn`، `block`.                                                                                                                                                |
| `RATE_LIMIT_AUTO_ENABLE`        | منطقي | `false`           | تمكين تحديد معدل الطلبات تلقائيًا استنادًا إلى أنماط الاستخدام.                                                                                                                                                   |
| `DISABLE_CONTEXT_WINDOW_CHECKS` | منطقي | `false`           | تخطي فحص OmniRoute المحلي لنافذة السياق / الحد الأقصى لرموز الإدخال للطلبات المباشرة ذات النموذج الواحد. تظل حدود الخدمات المزوّدة سارية.                                                                         |
| `CAPABILITY_FILTER_ENABLED`     | منطقي | `false`           | رفض الطلبات قبل إرسالها عندما يفتقر النموذج المستهدف إلى القدرات المطلوبة (الرؤية، الأدوات، المخرجات المنظَّمة، نافذة السياق). يحمي طلبات المزوّد الواحد المباشرة التي تتجاوز عامل تصفية التوافق في طبقة التجميع. |
| `RADAR_ENABLED`                 | منطقي | `false`           | تمكين وحدة OmniRoute Radar (شاشات موجز الكتالوج والمزامنة). تكون معطّلة افتراضيًا؛ ولا يؤدي تمكينها إلا إلى إتاحة واجهة المستخدم — إذ تظل مزامنة البيانات خيارًا منفصلًا يتطلب الاشتراك.                          |

### وقت التشغيل (32)

| المفتاح                                     | النوع | الافتراضي | إعادة التشغيل | الوصف                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ------------------------------------------- | ----- | --------- | ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `UNIVERSAL_CONTEXT_HANDOFF_ENABLED`         | منطقي | `true`    |               | إنشاء ملخصات للمحادثة وإدراجها عندما يؤدي التوجيه المركّب إلى تبديل النماذج. عطّله للتعامل مع عمليات تبديل النماذج بصورة مستقلة ومنع طلبات التسليم في الخلفية لجميع التركيبات الحالية والمستقبلية.                                                                                                                                                                                                                                                                                       |
| `RESPONSES_PASSTHROUGH_DROP_COMMENTARY`     | منطقي | `true`    |               | إسقاط عناصر المخرجات الداخلية الخاصة بمرحلة التعليق من تدفقات العبور الخاصة بـ Responses API قبل تمريرها إلى العملاء. عطّله لتلقي التعليقات الأولية من المصدر.                                                                                                                                                                                                                                                                                                                           |
| `OMNIROUTE_MCP_ENFORCE_SCOPES`              | منطقي | `true`    |               | فرض قيود النطاق على الوصول إلى أدوات MCP.                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `OMNIROUTE_MCP_COMPRESS_DESCRIPTIONS`       | منطقي | `false`   |               | ضغط أوصاف أدوات MCP لتقليل استخدام الرموز.                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `OMNIROUTE_ENABLE_RUNTIME_BACKGROUND_TASKS` | منطقي | `false`   |               | تمكين معالجة مهام الخلفية في وقت التشغيل.                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `OMNIROUTE_DISABLE_BACKGROUND_SERVICES`     | منطقي | `false`   | ✓             | تعطيل جميع خدمات الخلفية (تحديث الحصة والمزامنة وما إلى ذلك).                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `OMNIROUTE_RTK_TRUST_PROJECT_FILTERS`       | منطقي | `false`   |               | الوثوق بمرشحات RTK على مستوى المشروع دون تحقق.                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `OMNIROUTE_ENABLE_LIVE_WS`                  | منطقي | `true`    | ✓             | تشغيل خادم WebSocket للوحة المعلومات في الوقت الفعلي عند الاستيراد (المنفذ 20132 افتراضيًا).                                                                                                                                                                                                                                                                                                                                                                                             |
| `OMNIROUTE_CODEX_WS_ENABLED`                | منطقي | `true`    |               | السماح لـ Codex باستخدام نقل Responses عبر WebSocket. عند إيقافه، يعود Codex إلى HTTP Responses.                                                                                                                                                                                                                                                                                                                                                                                         |
| `OMNIROUTE_CODEX_APP_SERVER_ENABLED`        | منطقي | `true`    |               | السماح لـ Codex باستخدام نقل WebSocket JSON-RPC لخادم التطبيقات المحلي (codexTransport=app-server). عند إيقافه، تعود الاتصالات التي اختارت app-server إلى وسائل النقل الأخرى الخاصة بـ Codex.                                                                                                                                                                                                                                                                                            |
| `OMNIROUTE_EMERGENCY_FALLBACK`              | منطقي | `true`    |               | توجيه الطلبات التي استنفدت الميزانية إلى موفر/نموذج الرجوع الاحتياطي المجاني للطوارئ. (راجع [الرجوع الاحتياطي لميزانية الطوارئ](#emergency-budget-fallback) أدناه.)                                                                                                                                                                                                                                                                                                                      |
| `STREAM_RECOVERY_ENABLED`                   | منطقي | `false`   |               | تمكين إعادة المحاولة المبكرة والشفافة لتدفقات SSE المصدرية المبتورة قبل وصول أي بايتات استجابة إلى العميل.                                                                                                                                                                                                                                                                                                                                                                               |
| `STREAM_RECOVERY_MIDSTREAM_ENABLED`         | منطقي | `false`   |               | السماح باسترداد التدفق عبر إعادة الطلب ودمج الاستجابة بعد وصول البايتات بالفعل إلى العميل.                                                                                                                                                                                                                                                                                                                                                                                               |
| `STREAM_RECOVERY_TOOLCALL_ORDER_FIX`        | منطقي | `false`   |               | جعل متابعة منتصف التدفق آمنة لاستدعاءات الأدوات: عدم استئناف تدفق مقطوع مطلقًا بعد إصدار استدعاء أداة (قيد التنفيذ أو منتهي بالفعل مع finish_reason tool_calls)، والإغلاق بعد متابعة فارغة واحدة بدلًا من استهلاك الميزانية كاملة. عند الإيقاف: سلوك الإصدار القياسي.                                                                                                                                                                                                                    |
| `STREAM_EARLY_EOF_SIBLING_FAILOVER_ENABLED` | منطقي | `false`   |               | التحويل مرة واحدة إلى اتصال نظير عندما يُغلق تدفق SSE قبل إصدار أي إطار مفيد وبعد استنفاد إعادة المحاولة المحدودة على الاتصال نفسه؛ وفي حال عدم توفر نظير صالح للاستخدام، تُعاد استجابة `STREAM_EARLY_EOF` 502 الأصلية. متوقف افتراضيًا: يظل انتهاء الملف المبكر نهائيًا بعد إعادة المحاولة على الاتصال نفسه.                                                                                                                                                                            |
| `MODEL_CATALOG_INCLUDE_NAMES`               | منطقي | `true`    |               | تضمين حقول أسماء ملائمة للعرض في استجابات `/v1/models`. عطّله للعملاء الذين يتوقعون معرّفات النماذج فقط.                                                                                                                                                                                                                                                                                                                                                                                 |
| `MODELS_CATALOG_PREFIX_MODE`                | تعداد | `dual`    |               | يتحكم في كيفية إضافة البادئات إلى معرّفات النماذج في /v1/models. يصدر 'dual' (الافتراضي) بادئتي الاسم المستعار ومعرّف الموفر الأساسي معًا للتوافق مع الإصدارات السابقة. يصدر 'alias' بادئة الاسم المستعار القصيرة فقط (مثل ds-web/model، وليس deepseek-web/model). يصدر 'canonical' بادئة معرّف الموفر الكاملة فقط. القيم: `dual`، `alias`، `canonical`.                                                                                                                                 |
| `ARENA_ELO_SYNC_ENABLED`                    | منطقي | `true`    |               | تمكين المزامنة الدورية لتصنيفات ELO في لوحة صدارة Arena AI من أجل تصنيفات ذكاء النماذج.                                                                                                                                                                                                                                                                                                                                                                                                  |
| `EXPOSE_CC_DISCOVERY_ALIASES`               | منطقي | `false`   |               | الإعلان عن معرّفات النسخ المتطابقة `claude/<provider>/<model>` في `/v1/models` لكي يعرض اكتشاف نماذج بوابة Claude Code النماذج غير التابعة لـ Claude. المستوى العام للبوابة ثلاثية المستويات (متغير البيئة له الأولوية على تجاوز لوحة المعلومات). راجع [تهيئة Claude Code](../guides/CLAUDE-CODE-CONFIGURATION.md#discovery-aliases--surface-non-claude-models-in-the-model-picker).                                                                                                     |
| `NO_THINKING_ALIAS_ENABLED`                 | منطقي | `true`    |               | المفتاح الرئيسي للأسماء المستعارة للبوابة no-think/<provider>/<model>. عند التشغيل (الافتراضي): يعلن /v1/models عن متغير بلا تفكير لكل نموذج Claude مؤهل وقادر على التفكير، ويُعاد تحليل معرّف no-think/ المُرسل في الطلب إلى النموذج الحقيقي مع كبح الاستدلال. عند الإيقاف: لا يتم الإعلان عن أي متغيرات ويُعامل معرّف no-think/ مثل أي معرّف نموذج آخر غير معروف. يظل الاشتراك/إلغاء الاشتراك لكل نموذج عبر ModelSpec.noThinkingAlias مطبقًا أثناء تشغيل هذا الخيار.                   |
| `OMNIROUTE_DISABLE_THINKING_LEVEL_VARIANTS` | منطقي | `false`   |               | تعطيل إنشاء متغيرات مستوى التفكير (مثل -low و-medium- و-high-) في دليل /v1/models.                                                                                                                                                                                                                                                                                                                                                                                                       |
| `OMNIROUTE_CHAT_VIRTUAL_LANES`              | منطقي | `false`   | ✓             | تمكين مسارات قبول افتراضية تكيفية لكل مستأجر لإرسال الطلبات إلى الموفر (#9654): لن تؤدي طفرة طلبات أحد المستأجرين بعد الآن إلى أخطاء 503 لمستأجر آخر. تكون الأولوية لمتغير البيئة OMNIROUTE_CHAT_VIRTUAL_LANES على تجاوز لوحة المعلومات هذا؛ وتسري التغييرات عند إعادة تشغيل الخادم.                                                                                                                                                                                                     |
| `EXPOSE_FUNCTIONAL_GATEWAY_MIRRORS`         | منطقي | `false`   |               | الإعلان عن معرّفات النسخ المتطابقة <gateway-alias>/<model> في /v1/models للنماذج التي لا يملك مالكها الأساسي بيانات اعتماد نشطة، لكن توجد بوابة عبور ذات بيانات اعتماد نشطة تتولى توجيهها. تحذير: يؤدي تمكينه عالميًا إلى إضافة إدخالات في الدليل لجميع العملاء.                                                                                                                                                                                                                         |
| `NEWAPI_AGGREGATOR_BALANCE`                 | منطقي | `false`   |               | تمكين اكتشاف الرصيد للعُقد المتوافقة مع مجمّعات New-API / One-API / Sub2API. عند التمكين، ستعرض العُقد المتوافقة التي ضُبطت عليها علامة المجمّع رصيدها في لوحة المعلومات وفي توجيه الفحص المسبق للحصة.                                                                                                                                                                                                                                                                                   |
| `SERVER_OWNED_TOOL_LOOP_ENABLED`            | منطقي | `false`   |               | متابعة استدعاءات الأدوات غير المتدفقة التي يملكها الخادم حتى يعيد النموذج استجابة صالحة للاستخدام من قِبل العميل.                                                                                                                                                                                                                                                                                                                                                                        |
| `SEARCH_STATS_HIDE_DELETED_CONNECTIONS`     | منطقي | `false`   |               | لا تحتسب إحصاءات البحث وعمليات البحث الأخيرة إلا الموفرين الذين لا يزال لديهم اتصال نشط (يُحتسب دائمًا الموفرون الذين لا يتطلبون مفتاحًا مثل duckduckgo-free). عند إيقافه، يُحتفظ بكل صف بحث محفوظ يتضمن معرّف موفر.                                                                                                                                                                                                                                                                     |
| `FREE_BADGE_REQUIRES_PROVIDER_FREE_TIER`    | منطقي | `false`   |               | صفحات الموفرين في لوحة المعلومات: إظهار شارة «مجاني» فقط عند وجود إشارات يحترمها الموفر — مع تجاهل الاستدلال من اسم العرض وحقول المجانية غير المنطقية واللواحق :free لدى الموفرين المسجلين الذين لا يملكون فئة مجانية موثقة. عند إيقافه، يُحتفظ بقاعدة الشارة السابقة.                                                                                                                                                                                                                   |
| `RETRY_AFTER_PROVENANCE_ENABLED`            | منطقي | `false`   |               | في استجابات عدم التوفر المجمعة 429/503، إغفال `Retry-After` عندما لا يكون وقت إعادة المحاولة المستقبلي الفعلي معروفًا (بدلًا من قيمة اصطناعية قدرها 1s)، وإضافة `error.retry_after_provenance` (`signal` \| `none`)، والسماح لمسارات استنفاد التركيبة بقراءة تلميحات إعادة المحاولة النصية من أجسام JSON والنص العادي الواردة من المصدر. لا يظهر الحقل إلا في الاستجابات التي تنشئها `unavailableResponse()`؛ وتظل أجسام 429/503 الأخرى دون تغيير.                                       |
| `PROTECTED_PRIORITY_INFRA_502_ENABLED`      | منطقي | `false`   |               | عندما يؤدي هدف تركيبة من نوع `priority` وموسوم بأنه احتياطي عند استنفاد الحصة فقط إلى إيقاف التركيبة لسبب يمكن إثبات أنه لا يتعلق بالحصة (قاطع دائرة الموفر مفتوح، أو تخطٍ تنبؤي بسبب زمن الاستجابة)، تكون الاستجابة 502 بدلًا من 503 التي تبدو مرتبطة بالحصة. تظل حالات الحظر وفترة التهدئة وعدم التوفر والاستنفاد والتوقف بسبب حد التزامن على 503.                                                                                                                                     |
| `MISTRAL_AMBIGUOUS_401_SOFT_LOCKOUT`        | منطقي | `false`   |               | تكون استجابة Mistral 401 المجردة (`{"detail":"Unauthorized"}`، من دون إشارة مصادقة صريحة) متطابقة عند إلغاء المفتاح وعند استنفاد الحصة. عند التشغيل، تضع الاتصال في فترة تهدئة بدلًا من إيقافه بوصفه `expired`، بحد أقصى 3 مرات في الساعة لكل اتصال؛ أما المرة التالية فتوقفه، بحيث يستقر المفتاح الملغى في النهاية. متوقف افتراضيًا: تؤدي كل استجابة Mistral 401 مجردة إلى إيقاف الاتصال كما في السابق.                                                                                 |
| `XAI_OAUTH_LIVE_MODEL_DISCOVERY`            | منطقي | `false`   |               | جلب دليل نماذج xAI المباشر لاتصالات `xai-oauth` من `https://api.x.ai/v1/models` باستخدام رمز OAuth للحامل، بدلًا من البذرة الثابتة المجمّدة. متوقف افتراضيًا: يواصل `xai-oauth` تقديم البذرة الثابتة دون تغيير. عند حدوث أي خطأ في التحليل، يعود الاكتشاف إلى البذرة (لم يتم التحقق مما إذا كان x.ai يقبل رمز OAuth للحامل عند نقطة النهاية هذه).                                                                                                                                        |
| `BATCH_AND_FILE_AUTO_CLEANUP_ENABLED`       | منطقي | `false`   |               | السماح لعملية التنظيف التلقائي بحذف مهام Batch API النهائية (المكتملة/الفاشلة/الملغاة/منتهية الصلاحية) الأقدم من `OMNIROUTE_BATCH_RETENTION_DAYS`، إلى جانب نقاط التحقق لكل سطر، ومسح محتوى BLOB للملفات المرفوعة التي تجاوزت قيمة `expires_at` الخاصة بها. متوقف افتراضيًا: يحتفظ كل تثبيت حالي بهذه البيانات كما كان من قبل إلى أن يختار المشغّل الاشتراك. لا يتأثر مسار `DELETE /api/v1/batches/delete-completed` الذي يشغّله المشغّل في الحالتين — فهو عقد API عام منفصل وغير مشروط. |

### واجهة سطر الأوامر (5)

| المفتاح                               | النوع | الافتراضي | إعادة التشغيل | الوصف                                                                                                                                                                                                |
| ------------------------------------- | ----- | --------- | ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `CLI_COMPAT_ALL`                      | منطقي | `false`   | ✓             | تمكين وضع التوافق لجميع عملاء واجهة سطر الأوامر.                                                                                                                                                     |
| `MODEL_ALIAS_COMPAT_ENABLED`          | منطقي | `false`   |               | تمكين طبقة توافق الأسماء المستعارة للنماذج.                                                                                                                                                          |
| `PRICING_SYNC_ENABLED`                | منطقي | `false`   |               | تمكين المزامنة التلقائية لبيانات التسعير (يتطلب أيضًا متغير البيئة `PRICING_SYNC_ENABLED`).                                                                                                          |
| `OMNIROUTE_AUTO_SYNC_CODEX_PROFILES`  | منطقي | `false`   |               | بعد مزامنة نماذج المزوّد، إعادة كتابة ملفات ملفات التعريف ~/.codex/*.config.toml تلقائيًا من الكتالوج المباشر. لا يغيّر أبدًا إعداد Codex النشط/الافتراضي. معطّل افتراضيًا.                          |
| `OMNIROUTE_AUTO_SYNC_CLAUDE_PROFILES` | منطقي | `false`   |               | بعد مزامنة نماذج المزوّد، إعادة كتابة ملفات تعريف Claude Code في ~/.claude/profiles/<name>/settings.json تلقائيًا من الكتالوج المباشر. لا يغيّر أبدًا إعداد Claude النشط/الافتراضي. معطّل افتراضيًا. |

### السلامة (5)

| المفتاح                                   | النوع | الافتراضي | الوصف                                                                                                                                                                                                                                                             |
| ----------------------------------------- | ----- | --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `OMNIROUTE_DISABLE_LOCAL_HEALTHCHECK`     | منطقي | `false`   | تعطيل نقطة نهاية فحص سلامة المثيل المحلي.                                                                                                                                                                                                                         |
| `OMNIROUTE_DISABLE_TOKEN_HEALTHCHECK`     | منطقي | `false`   | تعطيل فحص سلامة التحقق من الرمز المميز.                                                                                                                                                                                                                           |
| `SKILLS_SANDBOX_NETWORK_ENABLED`          | منطقي | `false`   | تمكين الوصول إلى الشبكة في بيئة الحماية المعزولة للمهارات.                                                                                                                                                                                                        |
| `PROXY_HEALTH_BLOCKED_RESETS_STREAK`      | منطقي | `false`   | في عملية مسح سلامة الوكيل، يعيد الفحص الذي رفضه الهدف (401/403/429) ضبط سلسلة الإخفاقات المتتالية للوكيل. معطّل افتراضيًا: يظل الرفض محايدًا (#10654). تظل استجابة 5xx غير حاسمة في كلتا الحالتين؛ ولا يؤدي الرفض أبدًا إلى إزالة وكيل أو تعطيله أو إعادة تنشيطه. |
| `DB_HEALTHCHECK_STARTUP_DEFERRED_ENABLED` | منطقي | `false`   | تشغيل فحص تكامل/سلامة قاعدة البيانات عند بدء التشغيل بعد أن يبدأ الخادم في قبول الطلبات (عبر `setImmediate`) بدلًا من حظر بدء التشغيل حتى اكتماله (#13717). معطّل افتراضيًا: يُحظر بدء التشغيل تمامًا كما كان قبل طلب السحب هذا.                                  |

> [!NOTE]
> يضبط `INPUT_SANITIZER_BLOCK_THRESHOLD` واسمه المستعار القديم
> `INJECTION_GUARD_BLOCK_THRESHOLD` وضع `block` الخاص بـ
> `INJECTION_GUARD_MODE`، لكنهما متغيرا بيئة عاديان تقرؤهما
> [`src/shared/utils/injectionSeverity.ts`](../../src/shared/utils/injectionSeverity.ts)،
> وليسا علامتي ميزات: فلا يوجد لهما تجاوز في قاعدة البيانات ولا مفتاح تبديل في لوحة المعلومات. راجع
> [`ENVIRONMENT.md`](./ENVIRONMENT.md#4-security--authentication).

> [!NOTE]
> يشير عمود `إعادة التشغيل` إلى العلامات التي تحتوي على `requiresRestart: true` — تُحفَظ القيمة
> فورًا، لكنها لا تدخل حيز التنفيذ إلا بعد إعادة تحميل العملية. ترفض علامات
> التعداد أي قيمة خارج المجموعة المسموح بها (يُتحقق منها على جانب الخادم في كلٍ من
> `setFeatureFlagOverride()` ومعالج REST `PUT`).

---

## تبديل العلامات

### لوحة التحكم

انتقل إلى **لوحة التحكم → الإعدادات → علامات الميزات**
(`/dashboard/settings/feature-flags`). تدعم الشبكة
(`src/app/(dashboard)/dashboard/settings/components/FeatureFlagsGrid.tsx`)
ما يلي:

- **البحث** حسب المفتاح أو الوصف، و**التصفية** حسب الفئة (بالإضافة إلى عرض اصطناعي
  باسم **يتطلب إعادة التشغيل**).
- **مفتاح تبديل** للعلامات المنطقية و**قائمة منسدلة** لعلامات التعداد
  (`src/app/(dashboard)/dashboard/settings/components/FeatureFlagCard.tsx`).
- **شارة مصدر** لكل علامة — `DB` أو `ENV` أو `DEF` — توضّح مصدر
  القيمة الفعلية.
- زر **إعادة التعيين** (يظهر فقط للعلامات التي مصدرها `DB`) لإزالة التجاوز،
  وزر **إعادة تعيين جميع التجاوزات** في الأسفل.
- لافتة **إعادة تشغيل الخادم** عند تغيير علامة ذات `requiresRestart`.

### واجهة REST API

تمر جميع العمليات عبر مسار واحد:
[`src/app/api/settings/feature-flags/route.ts`](../../src/app/api/settings/feature-flags/route.ts).
تتطلب كل طريقة جلسة لوحة تحكم موثّقة (وإلا تُرجع `401`).

#### `GET /api/settings/feature-flags`

تُرجع كل علامة مع قيمتها الفعلية ومصدرها وملخص.

```jsonc
{
  "flags": [
    {
      "key": "REQUIRE_API_KEY",
      "label": "Require API Key",
      "description": "Require an API key for all incoming requests",
      "category": "security",
      "type": "boolean",
      "enumValues": null,
      "defaultValue": "false",
      "effectiveValue": "false",
      "source": "default", // "db" | "env" | "default"
      "requiresRestart": false,
      "warningLevel": "caution",
    },
    // ... جميع العلامات الـ 72
  ],
  "summary": {
    "total": 56,
    "active": 0,
    "inactive": 0,
    "overriddenByDb": 0,
    "overriddenByEnv": 0,
  },
}
```

#### `PUT /api/settings/feature-flags`

يضبط تجاوزًا واحدًا أو يزيله. المتن: `{ key: string; value?: string }`.
يؤدي حذف `value` إلى إزالة التجاوز (واستعادة قيمة متغير البيئة / القيمة الافتراضية).

```bash
# ضبط تجاوز في DB
curl -X PUT http://localhost:20128/api/settings/feature-flags \
  -H "Content-Type: application/json" \
  -d '{"key":"REQUIRE_API_KEY","value":"true"}'

# إزالة التجاوز (من دون "value")
curl -X PUT http://localhost:20128/api/settings/feature-flags \
  -H "Content-Type: application/json" \
  -d '{"key":"REQUIRE_API_KEY"}'
```

تُعيد الاستجابة قيمتي `effectiveValue`/`source` الجديدتين، وقيمتي `previousValue`/
`previousSource`، و`requiresRestart`. تُرفض المفاتيح غير المعروفة وقيم التعداد
الخارجة عن النطاق بالرمز `400`.

#### `DELETE /api/settings/feature-flags`

يمسح **جميع** تجاوزات DB دفعة واحدة، ويعيد كل علامة إلى قيمة متغير البيئة / القيمة
الافتراضية الخاصة بها. يُرجع `{ cleared: <count>, message: "..." }`.

> [!NOTE]
> لا تصبح العلامات ذات `requiresRestart: true` سارية إلا بعد إعادة تحميل العملية.
> يستدعي مسار إعادة التشغيل في لوحة التحكم `POST /api/restart` ثم يستطلع
> `GET /api/health/ping` حتى يعود الخادم إلى العمل.

---

## المسار الاحتياطي الطارئ للميزانية

يتحكم `OMNIROUTE_EMERGENCY_FALLBACK` (الفئة `runtime`، والقيمة الافتراضية `true`) في
مسار الرجوع المجاني الطارئ ضمن
[`open-sse/services/emergencyFallback.ts`](../../open-sse/services/emergencyFallback.ts).
عند تمكينه، تُوجَّه الطلبات التي تستنفد ميزانيتها إلى موفّر/نموذج احتياطي مجاني
بدلًا من فشلها مباشرةً. اضبطه على `false` (أو `0`) — عبر مفتاح التبديل في
لوحة التحكم، أو تجاوز في DB، أو متغير البيئة `OMNIROUTE_EMERGENCY_FALLBACK`
— لتعطيل هذا السلوك والسماح بفشل الطلبات التي استنفدت ميزانيتها.
(ظهر كمفتاح تبديل في لوحة التحكم ضمن طلبي الدمج #3741 / #3752.)

---

## انظر أيضًا

- [مرجع متغيرات البيئة](./ENVIRONMENT.md) — لمعظم العلامات متغير بيئة موثّق هناك
  يحمل الاسم نفسه (وتكون لتجاوز قاعدة البيانات الأولوية عليه).
- [`src/shared/constants/featureFlagDefinitions.ts`](../../src/shared/constants/featureFlagDefinitions.ts)
  — المصدر المعتمد لكل علامة.
- [`src/shared/utils/featureFlags.ts`](../../src/shared/utils/featureFlags.ts)
  — منطق تحديد القيم (`resolveFeatureFlag`، و`isFeatureFlagEnabled`،
  و`resolveAllFeatureFlags`).
- [`src/lib/db/featureFlags.ts`](../../src/lib/db/featureFlags.ts) — تخزين تجاوزات قاعدة البيانات
  بشكل دائم في نطاق الأسماء `feature_flags` ضمن جدول `key_value`.
